# Mini Project - Ticket Booking System

### Installation
```
python -m venv .venv
pip install -r requirements.txt
```
Run the MongoDB and Redis in localhost
```
docker-compose up -d
```
Run the Fast API backend
```
uvicorn app.main:app --reload
```
